# ---------------------------------------------------------------------------
# 模型生成工具链
#
# urdf/ 下的完整建模工具链、原始 CAD 导出件、渲染图,权威副本在
#   https://github.com/xinshang555/-urdf-
# 本仓库只保留跑仿真所必需的 car.xml + meshes,不重复那套工具链。
# ---------------------------------------------------------------------------
urdf/tools/
urdf/_src/
urdf/render/
urdf/*.urdf
urdf/*.md
urdf/view_mujoco.bat
urdf/view_mujoco.sh

# ---------------------------------------------------------------------------
# 建模源件
#
# model/car.STEP 是 CAD 源件，权威副本在 xinshang555/-urdf- 仓库。
# 跑仿真只需要 urdf/car.xml + meshes，不需要 STEP。
# ---------------------------------------------------------------------------
model/

# ---------------------------------------------------------------------------
# 非本项目文件
#
# test_skill_e2e.ps1 是 github-push 技能的自检脚本，与 LQR 无关。
# ---------------------------------------------------------------------------
test_skill_e2e.ps1

# ---------------------------------------------------------------------------
# 本地环境产物
# ---------------------------------------------------------------------------
__pycache__/
*.pyc
*.pyo
MUJOCO_LOG.TXT
muJoCo_log.txt

# ---------------------------------------------------------------------------
# 其他仓库的克隆 / 归档文件
#
# _repo/ 是 xinshang555/-urdf- 的本地克隆,自带 .git,
# 若纳入本仓库会变成嵌套仓库(而且外层只记录一个 gitlink,内容不进去)。
# ---------------------------------------------------------------------------
_repo/
*.zip
push_to_github.ps1

# ---------------------------------------------------------------------------
# 仿真产物
# ---------------------------------------------------------------------------
sim/result_*.png
cpp/build/
*.log
